version: '3.8'
services:
  react-phone-formatter:
    build: .
    ports: 
      #- '3000:3000'
      - '3001:3001'
      - '8080:80'
    volumes:
      #- ./src:/react-app/src
      - ./api:/api-server 
    environment:
      - CHOKIDAR_USEPOLLING=true
      - API_PORT=3001