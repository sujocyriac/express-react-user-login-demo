import { makeStyles } from "@mui/styles";

const styles = makeStyles({
  root: {
    maxWidth: "450px",
    margin: "0 auto",
    marginTop: "80px",
  },
  inputsBox: {
    marginTop: "20px",
  },
  inputs: {
    marginTop: "10px",
  },
  submitButton: {
    marginRight: "10px",
    marginBottom: "10px",
    float: "right",
  },
});

export default styles;
