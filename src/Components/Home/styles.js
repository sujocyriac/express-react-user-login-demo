import { makeStyles } from '@mui/styles';

const styles = makeStyles({
    usrBox: {
        position: 'absolute',
        top: '2px',
        right: '1em',
    },
    circle: {
        fontSize: '35px',
    },
    card: {
        marginTop: '150px',
        maxWidth: '400px',
    },
    text: {
        textAlign: 'center',
    },
});

export default styles;
